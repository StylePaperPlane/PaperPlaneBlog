# PaperPlane Blog Frontend

React + Vite frontend for PaperPlane Blog.

## Development

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` when you need to point the frontend at a custom backend URL.

## Build

```bash
npm run build
```
